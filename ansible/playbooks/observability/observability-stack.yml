---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         observability-stack
# title:       Observability · full stack
# category:    observability
# surface:     linux
# destructive: False

- name: "Observability · full stack"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'observability-stack'"
          - "Replace ansible/playbooks/observability/observability-stack.yml with the real playbook."
