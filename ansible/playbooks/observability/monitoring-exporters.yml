---
# Atom: node_exporter + postgres_exporter on PG hosts.
- name: Monitoring exporters
  hosts: postgres_servers
  become: true
  gather_facts: true
  roles:
    - role: node_exporter
    - role: postgres_exporter
