---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         managed-aws-aurora
# title:       AWS Aurora · provision
# category:    managed
# surface:     aws
# destructive: False

- name: "AWS Aurora · provision"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'managed-aws-aurora'"
          - "Replace ansible/playbooks/managed/aws-aurora.yml with the real playbook."
