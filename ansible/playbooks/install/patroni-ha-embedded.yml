---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         pg-ha-patroni
# title:       PostgreSQL · HA with Patroni (embedded etcd)
# category:    install
# surface:     postgres
# destructive: False

- name: "PostgreSQL · HA with Patroni (embedded etcd)"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'pg-ha-patroni'"
          - "Replace ansible/playbooks/install/patroni-ha-embedded.yml with the real playbook."
