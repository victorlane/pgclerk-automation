---
# Atom: install + configure a single PostgreSQL node (no Patroni, no replica).
- name: PostgreSQL standalone
  hosts: all
  become: true
  gather_facts: true
  vars:
    postgresql_ha_mode: standalone
  roles:
  - role: os_baseline
  - role: postgresql
