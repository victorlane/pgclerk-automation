---
# Atom: OS baseline only. Family-agnostic wrapper picks the right per-family
# role (rhel/debian) and lays down packages, sysctls, mounts, firewall.
- name: OS baseline
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: os_baseline
