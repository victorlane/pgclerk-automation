---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         os-baseline
# title:       OS · baseline hardening
# category:    install
# surface:     linux
# destructive: False

- name: "OS · baseline hardening"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'os-baseline'"
          - "Replace ansible/playbooks/install/os-baseline.yml with the real playbook."
