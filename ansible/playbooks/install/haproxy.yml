---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         haproxy
# title:       HAProxy · cluster LB
# category:    install
# surface:     linux
# destructive: False

- name: "HAProxy · cluster LB"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'haproxy'"
          - "Replace ansible/playbooks/install/haproxy.yml with the real playbook."
