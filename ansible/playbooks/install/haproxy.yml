---
# Atom: HAProxy in front of Patroni. Floating + read-only listeners.
- name: HAProxy
  hosts: haproxy_servers
  become: true
  gather_facts: true
  roles:
    - role: os_baseline
    - role: haproxy
