---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         reindex
# title:       PostgreSQL · reindex
# category:    maintenance
# surface:     postgres
# destructive: False

- name: "PostgreSQL · reindex"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'reindex'"
          - "Replace ansible/playbooks/maintenance/reindex.yml with the real playbook."
