---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         pg-dump-now
# title:       pg_dump · ad-hoc dump
# category:    maintenance
# surface:     postgres
# destructive: False

- name: "pg_dump · ad-hoc dump"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'pg-dump-now'"
          - "Replace ansible/playbooks/maintenance/pg-dump-now.yml with the real playbook."
