---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         analyze
# title:       PostgreSQL · ANALYZE
# category:    maintenance
# surface:     postgres
# destructive: False

- name: "PostgreSQL · ANALYZE"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'analyze'"
          - "Replace ansible/playbooks/maintenance/analyze.yml with the real playbook."
