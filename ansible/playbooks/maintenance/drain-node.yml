---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         drain-node
# title:       Cluster · drain node
# category:    maintenance
# surface:     linux
# destructive: False

- name: "Cluster · drain node"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'drain-node'"
          - "Replace ansible/playbooks/maintenance/drain-node.yml with the real playbook."
