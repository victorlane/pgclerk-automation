---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         add-database
# title:       PostgreSQL · add database
# category:    maintenance
# surface:     postgres
# destructive: False

- name: "PostgreSQL · add database"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'add-database'"
          - "Replace ansible/playbooks/maintenance/add-database.yml with the real playbook."
