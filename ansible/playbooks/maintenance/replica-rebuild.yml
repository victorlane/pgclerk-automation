---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         replica-rebuild
# title:       PostgreSQL · replica rebuild
# category:    maintenance
# surface:     postgres
# destructive: True

- name: "PostgreSQL · replica rebuild"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'replica-rebuild'"
          - "Replace ansible/playbooks/maintenance/replica-rebuild.yml with the real playbook."
