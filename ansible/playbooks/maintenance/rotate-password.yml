---
# Stub generated from meta/catalogue.yml — replace with real automation.
# key:         rotate-password
# title:       PostgreSQL · rotate password
# category:    maintenance
# surface:     postgres
# destructive: False

- name: "PostgreSQL · rotate password"
  hosts: "{{ pgm_target_hosts | default('all') }}"
  gather_facts: false
  tasks:
    - name: Announce stub dispatch
      ansible.builtin.debug:
        msg:
          - "pgclerk: dispatched stub for 'rotate-password'"
          - "Replace ansible/playbooks/maintenance/rotate-password.yml with the real playbook."
