---
# Amazon Linux 2023 ships PG natively via dnf; no PGDG repo. Native packages
# are named postgresql<ver> / postgresql<ver>-server, binaries land in
# /usr/bin/, and the systemd unit is just "postgresql.service".
#
# The native unit uses /var/lib/pgsql/data (no version subdir). We adopt
# that default verbatim -- no systemd drop-in needed.
postgresql_packages:
- "postgresql{{ postgresql_version }}-server"
- "postgresql{{ postgresql_version }}"
- "postgresql{{ postgresql_version }}-contrib"
- "python3-psycopg2"

postgresql_service: "postgresql"
postgresql_data_dir: "/var/lib/pgsql/data"
postgresql_config_dir: "{{ postgresql_data_dir }}"
postgresql_conf_path: "{{ postgresql_data_dir }}/postgresql.conf"
postgresql_hba_path: "{{ postgresql_data_dir }}/pg_hba.conf"
postgresql_install_log: "/var/log/postgresql_{{ postgresql_version }}_install_{{ ansible_hostname }}_{{ ansible_date_time.date | replace('-', '') }}.log"
postgresql_log_dir: "{{ postgresql_data_dir }}/{{ postgresql_log_dirname }}"
postgresql_bin_path: "/usr/bin"
