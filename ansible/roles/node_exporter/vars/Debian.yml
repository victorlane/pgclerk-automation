---
# Debian/Ubuntu ship prometheus-node-exporter in main. Binary lands at
# /usr/bin/prometheus-node-exporter (different name from upstream). We
# write our own systemd unit and ignore the package's shipped one.
node_exporter_packages:
  - prometheus-node-exporter

node_exporter_package_bin: /usr/bin/prometheus-node-exporter
