[postgres_primary]
db1.example.com ansible_host=192.168.56.10

[postgres_replicas]
# db2.example.com ansible_host=192.168.56.11
# db3.example.com ansible_host=192.168.56.12

[etcd_servers]
# Populated only when patroni_dcs_mode=external. When embedded, etcd runs
# on the postgres_servers hosts themselves.

[backup_servers]
# Populated only when pgbackrest_repo_type includes 'server'.

[postgres_servers:children]
postgres_primary
postgres_replicas

[postgres_servers:vars]
ansible_user=opc
ansible_ssh_private_key_file=~/.ssh/id_rsa
ansible_become=true
